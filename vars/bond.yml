# List of bond slaves interface and assigned vlan/s
l2_bond:
  rack01:
    - { slaves: swp1, vids: vlan15 }
    - { slaves: swp2, vids: vlan16 }
    - { slaves: swp3, vids: vlan38 }
  rack02:
    - { slaves: swp1, vids: vlan38 }
    - { slaves: swp2, vids: vlan39 }
    - { slaves: swp3, vids: vlan15 }
