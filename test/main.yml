# - hosts: all
#   gather_facts: false
#   tasks:
#   - set_fact:
#       test01: "{{ 'loopback' | get_config }}"

  # - set_fact:
  #     test02: "{{ test01.split('\n') | reject('equalto', '') | list }}"
  # - nclu:
  #     commands:
  #       - show configuration commands
  #   register: show
  # - set_fact:
  #     test01: "{{ show['msg'] | regex_findall('net add vxlan \\w+.*')}}"
